# Unreviewed draft, preserved rather than reviewed

`v1-draft.json` existed on one machine, untracked, in no repository, and 404s at its own `$id`.
It is committed here to stop it being lost, not because it has been reviewed or approved.

Its own title says `DRAFT, NOT FOR ISSUANCE`. Take that at face value.

**Before this is ever served:** per `SCHEMA_POLICY.md`, a schema URL is immutable once published
and referenced. A file whose `$id` already names a public URL is one deploy away from being
permanent, so it gets the same review as any mint, and the pre-deploy byte-identity gate applies.
